sudo: required

branches:
  only:
    - master

language: C

env:
 - ROUTER_NAME=hsl
 - ROUTER_NAME=waltti
 - ROUTER_NAME=finland

services:
 - docker

install:
 - true

script:
 - ./build-docker-image.sh

before_deploy:
 - echo "start deploying ..."
