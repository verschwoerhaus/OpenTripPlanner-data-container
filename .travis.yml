sudo: required

branches:
  only:
    - master

services: docker

install: true

language: C

jobs:
  include:
   - stage: build
     env: ROUTER_NAME=hsl
     script: ./prebuild.sh
   - stage: build
     env: ROUTER_NAME=waltti
     script: ./prebuild.sh
   - stage: build
     env: ROUTER_NAME=finland
     script: ./prebuild.sh

   - stage: test
     env: ROUTER_NAME=hsl
     script: ./build-docker-image.sh
   - stage: test
     env: ROUTER_NAME=waltti
     script: ./build-docker-image.sh
   - stage: test
     env: ROUTER_NAME=finland
     script: ./build-docker-image.sh

   - stage: deploy
     env: ROUTER_NAME=hsl
     script: ./deploy.sh
   - stage: deploy
     env: ROUTER_NAME=waltti
     script: ./deploy.sh
   - stage: deploy
     env: ROUTER_NAME=finland
     script: ./deploy.sh
